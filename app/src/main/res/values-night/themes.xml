<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base nocturno compatible con Material -->
    <style name="Theme.TaskMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores personalizados para modo oscuro -->
        <item name="colorPrimary">@color/orange_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor" tools:ignore="NewApi">?attr/colorPrimary</item>
    </style>
</resources>
